import QtQuick
import QtQuick.Controls

import "Pages"

ApplicationWindow {
    id: root
    width: 400
    height: 600
    visible: true
    title: "Plazma"
    visibility: Window.Windowed

    PageStart {}
}